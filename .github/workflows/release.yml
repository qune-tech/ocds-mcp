name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            binary: vergabe-mcp
            asset: vergabe-mcp-linux-x86_64
          - target: aarch64-apple-darwin
            os: macos-latest
            binary: vergabe-mcp
            asset: vergabe-mcp-macos-arm64
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            binary: vergabe-mcp.exe
            asset: vergabe-mcp-windows-x86_64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build
        run: cargo build --release --target ${{ matrix.target }} -p vergabe-mcp

      - name: Package (Unix)
        if: matrix.os != 'windows-latest'
        shell: bash
        run: |
          cp target/${{ matrix.target }}/release/${{ matrix.binary }} vergabe-mcp
          tar czf ${{ matrix.asset }}.tar.gz vergabe-mcp

      - name: Package (Windows)
        if: matrix.os == 'windows-latest'
        shell: bash
        run: |
          cp target/${{ matrix.target }}/release/${{ matrix.binary }} vergabe-mcp.exe
          7z a ${{ matrix.asset }}.zip vergabe-mcp.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset }}
          path: ${{ matrix.asset }}.*

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            vergabe-mcp-linux-x86_64.tar.gz
            vergabe-mcp-macos-arm64.tar.gz
            vergabe-mcp-windows-x86_64.zip
